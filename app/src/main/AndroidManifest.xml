<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.delhipolice.avishigoyal.delhipolice">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Police.VendorNames"></activity>
        <activity android:name=".Police.PoliceStatusList" />
        <activity android:name=".SplashScreen.SlpashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".Complains.ComplainLodge"
            android:label="@string/title_activity_complain_lodge"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Complains.OCR.OcrCaptureActivity" />
        <activity
            android:name=".Complains.ComplainStatus.PopWindow"
            android:label="Status"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".Complains.OCR.LoggedComplain" />
    </application>

</manifest>